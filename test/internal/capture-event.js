var Capture = require('../../src/capture');

exports.captureevent = {

	/*"initialize": function (test) {
		test.expect(2);
		test.ok(true, 'should pass');
		
		var capture = new Capture();
		var cEvent = capture.addCaptureEvent({});

		// this is a little wierd,
		// should unique cids be specific to instances?
		test.equal(cEvent.cid, 'ce2', 'cid should be ce2');

		test.done();
	}*/

};